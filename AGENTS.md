# 云依助手项目约定

## 用户明确要求的交付流程

每次完成本项目的代码修改，都要同步更新说明、生成包含最新代码的 Windows 安装包，并将代码和安装包一起提交、推送到 GitHub；不要只交付本地源码后再次询问是否打包。用户当次明确要求只讨论、只检查、不发布或暂缓打包时，以当次要求为准。

1. 发布前检查工作区和远程最新提交，保留用户及远程已有更改，禁止强制推送。
2. 递增桌面版本号，同步 CMake、前端 package/lock、Windows 资源、安装器默认版本、Application 标记、README 和更新日志。
3. 运行与修改相关的构建/测试；构建前端后，执行 Release 原生构建，确保最新 UI 已复制到客户端，再生成安装包。
4. 同步 `web/public/updates/desktop.json` 的版本、时间、下载地址以及从实际安装包计算的 SHA-256 和大小。
5. 提交相关源码、说明、测试和 `out/dist/CloudYiAssistant-Setup-<版本>.exe`，推送并核对远程提交已同步。不要顺带提交无关改动。
6. 最终回复提供版本、提交编号、本地安装包链接和 GitHub 下载链接。若构建、打包或推送失败，必须明确说明哪一步未完成，不能声称已同步。

## 实现与素材边界

- 原生代码优先复用 C；需要 C++ 时使用 C++11，不引入 C++17 要求。
- 前端使用 React 18 和 SCSS Modules，不为视觉调整新增捆绑字体。
- 私人照片及其衍生角色不作为独立源码素材提交；已有私有角色只按既有流程进入生成的客户端/安装包。
- 不为验证发布而卸载软件、清除用户数据或停止无关进程。安装/卸载实机测试与构建测试须明确区分。
